[request]
method = "GET"
url = "{{base_url}}/{{api_prefix}}/users"

[headers]
Authorization = "Bearer {{auth_token}}"
X-Trace-ID = "TRACE-{{random}}"

[extract]
first_user_id = "0.id"
first_user_name = "0.username"

[assert]
status = 200
json = { "0.id" = { "exists" = true } }
