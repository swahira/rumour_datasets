[request]
method = "GET"
url = "{{base_url}}/{{api_prefix}}/users/:first_user_id"

[headers]
Authorization = "Bearer {{auth_token}}"

[assert]
status = 200
json = { "username" = { "equal" = "{{first_user_name}}" } }
