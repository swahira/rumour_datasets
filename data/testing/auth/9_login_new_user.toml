[request]
method = "POST"
url = "{{base_url}}/auth/login"

[body]
type = "json"
raw = """
{
  "email": "{{registered_email}}",
  "password": "password123"
}
"""

[variables]
registered_email = "new_healed_user@example.com"

[[dependencies]]
consumer_var = "registered_email"
producer_id = "0_register.toml"
