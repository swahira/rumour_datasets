[request]
method = "POST"
url = "{{base_url}}/auth/login"

[body]
type = "json"
raw = """
{"email": "{{healed_email}}", "password": "password123"}
"""

[[dependencies]]
consumer_var = "healed_email"
producer_id = "0_register.toml"

[extract]
# Namespaced variable
accessToken = "token"
