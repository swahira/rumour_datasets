[request]
method = "GET"
url = "{{base_url}}/auth/admin-only"

[headers]
# Using the token that was extracted by 1_login.toml (which is a standard user token)
Authorization = "Bearer {{accessToken}}"
