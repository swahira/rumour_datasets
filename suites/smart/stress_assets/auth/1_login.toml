[request]
method = "POST"
url = "{{base_url}}/{{api_prefix}}/auth/login"

[body]
type = "json"
raw = """
{
  "email": "{{test_user}}@example.com",
  "password": "{{test_pass}}"
}
"""

[extract]
auth_token = "token"

[assert]
status = 200
json = { "token" = { "exists" = true } }
